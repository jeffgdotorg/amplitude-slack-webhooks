## hookdump
I needed a quick and dirty way to see what was getting posted to a webhook receiver, so I wrote this tiny Flask app.

***Do not use in production environments***

### Setup
```sh
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

### Running
```sh
./run-app.sh
```
